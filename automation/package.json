{
  "name": "superconnector-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 7 trusted monthly automation, run by .github/workflows/monthly-automation.yml via the Firebase Admin SDK. Deliberately a SEPARATE package from the main app (which never imports anything here) so firebase-admin — a Node-only dependency — never risks being pulled into the browser bundle.",
  "scripts": {
    "start": "tsx src/run.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "googleapis": "^178.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "~6.0.2"
  }
}
